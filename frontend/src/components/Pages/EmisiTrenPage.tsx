"use client";
import { EMISSION_DEFINITIONS } from "@/constants/emissions";
export default function EmisiTrenPage() { return <div className="page-container"><div className="page-header-section"><span>RINGKASAN</span><h1>Emisi & Tren</h1><p>Ringkasan agregat akan tersedia setelah endpoint analitik terhubung.</p></div><div className="page-card-grid">{EMISSION_DEFINITIONS.map(({ key, label }) => <div className={`page-card summary-metric pollutant-${key}`} key={key}><span><i className="pollutant-dot" />{label}</span><strong>--</strong><small>Belum tersedia</small></div>)}</div><div className="page-card"><div className="card-header"><strong>Koridor prioritas</strong><span className="unavailable-badge">BELUM TERSEDIA</span></div><div className="unavailable-state">Ranking segmen dan tren historis menunggu endpoint agregat.</div></div></div>; }
