# ============================================================
# EcoTraffic GIS — Backend (FastAPI) Dependencies
# Used by: backend container (Dockerfile)
# Python 3.10+
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- WebSocket ---
websockets==12.0

# --- Database ---
sqlalchemy==2.0.30
asyncpg==0.29.0          # async PostgreSQL driver
psycopg2-binary==2.9.9   # sync driver (used by Alembic)
alembic==1.13.1
geoalchemy2==0.15.1      # PostGIS geometry types for SQLAlchemy

# --- Task Queue ---
celery==5.4.0
redis[asyncio]>=5.0.0,<6.0.0

# --- Pydantic ---
pydantic>=2.7.0,<3.0.0
pydantic-settings==2.2.1

# --- HTTP / Streaming ---
httpx==0.27.0
aiohttp==3.9.5
requests==2.32.3

# --- Environment ---
python-dotenv==1.0.1
python-multipart==0.0.9

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6

# --- Data Processing ---
pandas==2.2.2