# ============================================================
# EcoTraffic GIS — CV Worker Dependencies
# Used by: worker container (Dockerfile.worker)
# Python 3.10+
# Heavy: includes PyTorch via ultralytics (~2.5GB)
# ============================================================

# --- Computer Vision ---
ultralytics==8.2.0                   # YOLOv8 (pulls in PyTorch automatically)
opencv-python-headless==4.9.0.80     # headless — no GUI needed on server

# --- Task Queue ---
celery==5.4.0
redis==5.0.4

# --- Database ---
sqlalchemy==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9
geoalchemy2==0.15.1

# --- Pydantic ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- HTTP / Streaming ---
aiohttp==3.9.5

# --- Environment ---
python-dotenv==1.0.1